// Dotted-globe point cloud (land masses of Earth), sampled as [id, lat, lon].
// `id` acts as a density rank: rendering only points with id < threshold keeps
// the sphere legible. Data is decorative; see GlobeBackground for usage.
/* eslint-disable */
// prettier-ignore
export const GLOBE_POINTS: readonly (readonly [number, number, number])[] = [
];
